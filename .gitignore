node_modules
dist
.DS_Store
*.log
.env
.env.local
.vite

# --- secrets & local agent scratch (security hardening 2026-08-06) ---
.env
.env.*
!.env.example
!.env.sample
.agents/
ORIGINAL_REQUEST.md
*.pem
*.key
.claude/settings.local.json

# generated art: the atlas in public/art is the artifact, the raw frames are
# 29 MB of regenerable intermediates (npm run art)
art/raw/

# banner render output (assets/*.jpg are the committed artifacts)
build/
